let users:string[]=[];
if (users.length==0) {
    console.log(`there is no user / empty`);
    
}



// let usernames: string[] = [];

// if (usernames.length === 0) {
//     console.log("We need to find some users!");
// } else {
//     // Greet users
// }
// // Removing all usernames ensures the message "We need to find some users!" is printed.